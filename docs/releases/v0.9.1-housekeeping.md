# Release v0.9.1-housekeeping

> 发布对象：tag `v0.9.1-housekeeping` → commit `8494623`（已推远端）｜ 起草：2026-09-08 ｜ 状态：**待发布**（Release 说明页未在 GitHub 创建）

## aing v0.9.1 — Housekeeping（修缮版）

v0.9.0 之后的清理增量：源码 review 发现的问题全部清零，外加一处新环境友好性改进。单提交（`8494623`），3 文件 +5/−2，无行为变更。

### 变更清单

1. **`src/knowledge-store.js`**：删除 `init()` 中重复的 `_initTables()` 调用（幂等无碍但属冗余，review Bug #1）
2. **`src/tri-path-orchestrator.js`**：用法注释 `node tri-path-orchestrator.js熔断` 缺空格——复制即报错（review Bug #2）
3. **`verify-deploy.js`**：新增 C5 前置预检提示——`models/` 语义模型缺失时，验收开始前先打印预告（指向 `setup-vectors.ps1`），新环境首跑不再被一串红报错当第一印象；模型就位时零行为变化

### 升级

```bash
git pull        # main 16889c0 → 8494623，纯快进
node verify-deploy.js
```

无迁移、无配置变更、无依赖变更；数据库与种子文档不受影响。

### 校验

| 对象 | 值 |
|---|---|
| tag | `v0.9.1-housekeeping` |
| commit | `849462300ad14df6553187e8353f386088ce08c2` |
| 质量门 | verify-deploy ALL GREEN + self-test ALL GREEN（提交前实测） |
